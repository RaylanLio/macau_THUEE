课程：计算机程序设计基础（1）
内容：有关递归函数中打印排列问题
学号：3b1cbfed31422cde4a28252e866b449a
姓名：韩昀初
发布时间:2019-12-05 22:09:01
最后回复：孙甲松
回复时间：2019-12-05 22:37:35


This is a real-name registration system.Please abide by the relevant laws and regulations of the state when publishing topics or doing other operations.







Topic有关递归函数中打印排列问题



Reply









韩昀初




#include<stdio.h> #include<stdlib.h> #include<string.h> #pragma warning(disable:4996) void bubsort(int p[], int n) {     int m, k, j, i;     int d;     k = 0; m = n - 1;     while (k < m)     {         j = m - 1; m = 0;         for (i = k; i <= j; i++)             if (p[i] > p[i + 1])             {                 d = p[i]; p[i] = p[i + 1]; p[i + 1] = d; m = i;             }         j = k + 1; k = 0;         for (i = m; i >= j; i--)             if (p[i - 1]> p[i])             {                 d = p[i]; p[i] = p[i - 1]; p[i - 1] = d; k = i;             }     }     return; } void permutate(int a[], int n) {     int i, j;     int* b;     if (n > 1)     {         bubsort(a, n);         for (i = 0; i < n; i++)         {             a[0] = a[i];             b = a + 1;             permutate(b, n - 1);             for (j = 0; j < n - 1; j++)             {                 printf("%d ", a[0]);                 printf("%d ", b[j]);             }             a = b - 1;         }     }     else printf("%d", a[0]); } main() {     int* a;     int n,i;     printf("请输入元素个数:\n");     scanf("%d", &n);     printf("请输入这些互不相同的元素:\n");     a = (int*)malloc(sizeof(int) * n);     if (a == NULL)     {         printf("can't get memory!\n");         exit(1);     }     for (i = 0; i < n; i++)         scanf("%d",&a[i]);     permutate(a, n);     free(a); }
老师好，这个程序想实现将一组数的所有排列按字典顺序打印输出，我先利用冒泡排序排出第一个排列，在递归函数中每次让第一个元素分别与后面元素依次交换，对新获得的数组的后面n-1个元素进行排列处理，再打印整个排列。
请问应该如何在这个递归函数中逐行打印每一个排列？



Reply

Starter： 2019-12-05 22:09









孙甲松






 
 
   a[0] = a[i]; 不是把第一个元素 a[0] 覆盖了吗？没有让第一个元素分别与后面元素依次交换呀。 你重新理一下思路。
 




Reply(0)
Collapse

1#：2019-12-05 22:37






Switch to Editor Field
Switch to Textbox Field












Add Attach.(1G limited)




























likes


					        		Replies  
					        		1


					        		Views
			        		  		
			        		  		  
			        		  		  
			        		  		   35
			        		  		  
			        		  		








ReplySwitch to Editor FieldSwitch to Textbox Field







Add Attach.(1G limited)



















