课程：计算机程序设计基础（2）
内容：第四次作业附加题
学号：c9d7cf6ba57f955a031395e732028306
姓名：王潇然
发布时间:2020-03-30 23:55:43
最后回复：孙甲松
回复时间：2020-03-31 11:30:05


This is a real-name registration system.Please abide by the relevant laws and regulations of the state when publishing topics or doing other operations.







Topic第四次作业附加题



Reply









王潇然




程序编写如下：
#include<iostream>
#include<cstdlib>
#include<cmath>
#include<cstring>
#include<cstdio>
using namespace std;
 
class List;
class Listnode
{
    friend class List;
public:
    Listnode(int x, Listnode* n) { d = x;next = n; }
private:
    int d;
    Listnode* next;
};
 
class List
{
public:
    List(Listnode* h1, Listnode* p1) { head = h1;p = p1; }
    int insert(int x);
    int bubsort();
    int Print();
    Listnode* head, * p;
    int n = 0;
};
 
int List::insert(int x)
{
    Listnode* q = new Listnode(1, NULL);
    p = new Listnode(x,NULL);
    /*p->d = x;
    p->next = NULL;*/
    if (head == NULL)
    {
         head = p;
         q = head;
    }
    else 
    {
         q->next = p;
         q = p;
    }
    n++;
    return 1;
}
 
int List::bubsort()
{
    int i;
    Listnode* t,*m;
    for (i = 0;i <= n;i++)
    {
         t = head;
         while (t != NULL)
         {
             if ((t->d) > (t->next->d))
             {
                  m = t;
                  t = t->next;
                  t->next = m;
             }
             t = t->next;
         }
         
    }cout << "bubsort!" << endl;
    return 1;
}
 
int List::Print()
{
    int i;
    for(i=0;i<n;i++)
    {
         cout << head->d << endl;
         head = head->next;
    }
    return 1;
}
 
int main()
{
    int x;
    cout << "输入若干正整数，以0为结束：" << endl;
    cin >> x;
    Listnode p(x, NULL);
    List A(&p, NULL);
    while (x != 0)
    {
         A.insert(x);
         cin >> x;
    }
    A.Print();
    A.bubsort();
    A.Print();
    return 0;
}
 
 
输出结果如下：
输入若干正整数，以0为结束：
123456732 0
123456732
bubsort!
显示“引发异常；读取访问权限冲突，this->head is nullptr”
 
发现问题：只能读入并打印出第一个数；显示“head”是空指针
 
 
 



Reply

Starter： 2020-03-30 23:55









孙甲松






 
 
  <p>编译结果:&nbsp;</p> 
  <p>&nbsp; &nbsp; test.cpp(26) : error C2864: “List::n”: 只有静态常量整型数据成员才可以在类中初始化</p> 
  <p>List类中：</p> 
  <p>&nbsp; &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;int&nbsp;n = 0;</p> 
  <p>成员n不能在类中初始化，你是怎么运行的？</p> 
 




Reply(0)
Collapse

1#：2020-03-31 11:30






Switch to Editor Field
Switch to Textbox Field












Add Attach.(1G limited)




























likes


					        		Replies  
					        		1


					        		Views
			        		  		
			        		  		  
			        		  		  
			        		  		   42
			        		  		  
			        		  		








ReplySwitch to Editor FieldSwitch to Textbox Field







Add Attach.(1G limited)



















