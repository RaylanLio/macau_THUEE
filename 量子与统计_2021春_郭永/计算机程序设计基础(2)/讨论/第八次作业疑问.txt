课程：计算机程序设计基础（2）
内容：第八次作业疑问
学号：356cb20a3c935fe2cf4332d09eb8cc4f
姓名：任少睿
发布时间:2020-04-11 20:34:04
最后回复：孙甲松
回复时间：2020-04-12 14:03:27


This is a real-name registration system.Please abide by the relevant laws and regulations of the state when publishing topics or doing other operations.







Topic第八次作业疑问



Reply









任少睿




下面这个程序（第三题），确实能够运行出结果来，但是运行的时候不知道为什么要等很长时间（前两道题目的运行时间基本不需要等待，这个运行起来要等很久，大概要二十多秒才能运行出结果），我想问是因为哪里多绕了，有什么加速的办法吗？还是目前只能编写到这一步，没法再加速了？谢谢！
#include<iostream> #include<cmath> using namespace std; class Point { public:     Point(double a=0,double b=0):x(a),y(b){cout<<"Point构造函数被调用"<<endl;}     virtual ~Point(){cout<<"Point析构函数被调用"<<endl;};     Point(const Point &p)     {         this->x=p.x;         this->y=p.y;         cout<<"Point复制构造函数被调用"<<endl;     }     virtual double GetArea()     {         return 0;     } private:     double x,y; }; class Circle:virtual public Point { public:     virtual ~Circle(){cout<<"Circle析构函数被调用"<<endl;}     Circle(const Circle &c):Point(c),R(c.R){cout<<"Circle复制构造函数被调用"<<endl;}     virtual double GetArea()     {         return 3.1415926*R*R;     }     double RR()     {         return R;     }     Circle(double a=0,double b=0,double c=0):Point(a,b),R(c){cout<<"Circle构造函数被调用"<<endl;} private:     double R; }; class Square:virtual public Point { public:     Square(double a=0,double b=0,double c=0):Point(a,b),L(c){cout<<"Square构造函数被调用"<<endl;}     Square(Square &r):Point(r),L(r.L){cout<<"Square复制构造函数被调用"<<endl;}     virtual double GetArea()     {         return L*L;     }     double HH()     {         return L;     }     virtual ~Square(){cout<<"Square析构函数被调用"<<endl;} private:     double L; }; class Margin:public Circle,public Square { public:     Margin(double a,double b,double c,double d):Point(a,b),Circle(1,2,c),Square(1,2,d){cout<<"Margin构造函数被调用"<<endl;}     Margin(Margin &m):Point(m),Circle(m),Square(m){cout<<"Margin复制构造函数被调用"<<endl;}     ~Margin(){cout<<"Margin析构函数被调用"<<endl;}     virtual double GetArea()     {         if(Square::HH()<sqrt(2.0)*Circle::RR())             return -Square::GetArea()+Circle::GetArea();         if(Square::HH()>2*Circle::RR())             return Square::GetArea()-Circle::GetArea();         cout<<"给出数据不满足要求！"<<endl;         return 0;     } }; void f(Point *a) {     cout<<"面积为"<<a->GetArea()<<endl; } int main() {     Point *q[]={new Margin(1,2,3,4),new Margin(3,4,5,6)};     for(int i=0;i<=1;i=i+1)     {         f(q[i]);     }     for(int i=0;i<=1;i=i+1)     {         delete q[i];     }     return 0; }  



Reply

Starter： 2020-04-11 20:34









孙甲松






 
 
  <p>我在VS2008上运行起来非常快呀，几乎没有等待时间，运行结果：</p> 
  <p>Point构造函数被调用<br> Circle构造函数被调用<br> Square构造函数被调用<br> Margin构造函数被调用<br> Point构造函数被调用<br> Circle构造函数被调用<br> Square构造函数被调用<br> Margin构造函数被调用<br> 面积为12.2743<br> 面积为42.5398<br> Margin析构函数被调用<br> Square析构函数被调用<br> Circle析构函数被调用<br> Point析构函数被调用<br> Margin析构函数被调用<br> Square析构函数被调用<br> Circle析构函数被调用<br> Point析构函数被调用<br> 请按任意键继续. . .</p> 
  <p>&nbsp;</p> 
  <p>你再试一下，看是否还是这样。</p> 
 




Reply(0)
Collapse

1#：2020-04-12 14:03






Switch to Editor Field
Switch to Textbox Field












Add Attach.(1G limited)




























likes


					        		Replies  
					        		1


					        		Views
			        		  		
			        		  		  
			        		  		  
			        		  		   79
			        		  		  
			        		  		








ReplySwitch to Editor FieldSwitch to Textbox Field







Add Attach.(1G limited)



















